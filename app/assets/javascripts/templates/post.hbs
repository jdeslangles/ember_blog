<h1>{{title}}</h1>
<h2>by {{author.name}} <small class="muted">
  (posted {{format-date date}}{{#if lastUpdatedAt}}, updated{{format-date lastUpdatedAt}}{{/if}})
</small></h2>
<hr>

<div>
  {{format-markdown body}}
</div>